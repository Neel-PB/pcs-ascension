

## Update Override Vol KPI Placeholder Labels

### Change

In `src/pages/staffing/StaffingSummary.tsx`, update two placeholder strings:

1. **Line 138**: Change `"Select Dept"` to `"Select Department"`
2. **Lines 143 and 150**: Change `"--"` to `"No Override Found"`

No other files need changes -- the `KPICard` component already handles these as placeholder text with the small muted styling.

